<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ConEd.Views.Controls.TabItem"
             x:Name="RootControl">

    <VerticalStackLayout Padding="10,5">

        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="CommonStates">
                <VisualState x:Name="Normal">
                    <VisualState.Setters>
                        <Setter Property="Opacity" Value="1.0" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="PointerOver">
                    <VisualState.Setters>
                        <Setter Property="Opacity" Value="0.7" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <Label x:Name="TabLabel"
               Text="{Binding Source={x:Reference RootControl}, Path=Title}" 
               TextColor="#B3D4F0" 
               FontSize="14" 
               HorizontalOptions="Center"
               Margin="0,0,0,8" />

        <BoxView x:Name="ActiveIndicator" 
                 HeightRequest="4" 
                 Color="Transparent" 
                 CornerRadius="2" />

        <VerticalStackLayout.GestureRecognizers>
            <TapGestureRecognizer Command="{Binding Source={x:Reference RootControl}, Path=Command}" 
                                  CommandParameter="{Binding Source={x:Reference RootControl}, Path=CommandParameter}" />
        </VerticalStackLayout.GestureRecognizers>

        <VerticalStackLayout.Triggers>
            <DataTrigger TargetType="VerticalStackLayout" 
                         Binding="{Binding Source={x:Reference RootControl}, Path=IsActive}" Value="True">
                <Setter TargetName="TabLabel" Property="Label.TextColor" Value="White" />
                <Setter TargetName="TabLabel" Property="Label.FontAttributes" Value="Bold" />
                <Setter TargetName="ActiveIndicator" Property="BoxView.Color" Value="White" />
            </DataTrigger>
        </VerticalStackLayout.Triggers>

    </VerticalStackLayout>
</ContentView>